class MyCustomSorter:

	static func sort(a, b):
		var result = false
		
        if (Parent.position.distance_to(b.global_position) < Parent.position.distance_to(a.global_position)):
            result = true
#		return false
#		print (a)
		return result
	
	pass
	
